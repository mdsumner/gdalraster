% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdal_mdim-2.R
\name{mdim_read}
\alias{mdim_read}
\title{Read data from a multidimensional array}
\usage{
mdim_read(
  dsn,
  array_name,
  start = NULL,
  count = NULL,
  step = NULL,
  decode = TRUE,
  open_options = NULL
)
}
\arguments{
\item{dsn}{Character string containing the data source name.}

\item{array_name}{Character string giving the name of the array to read.}

\item{start}{Optional numeric vector of 0-based start indices for each
dimension. Default reads from the origin.}

\item{count}{Optional numeric vector of element counts for each
dimension. Default reads all remaining elements.}

\item{step}{Optional numeric vector of step sizes for each dimension.
Default step is 1.}

\item{decode}{Logical. \code{TRUE} (default) to apply CF convention decoding:
nodata values are replaced with \code{NA} and scale/offset are applied.}

\item{open_options}{Optional character vector of \code{NAME=VALUE} pairs
specifying dataset open options.}
}
\value{
A numeric, integer, or raw vector with a \code{gis} attribute
containing a list with components: \code{type}, \code{dim}, \code{dim_names},
\code{coords}, \code{srs}, \code{datatype}, and optionally \code{nodata}, \code{scale},
\code{offset}, \code{bbox}.
}
\description{
\code{mdim_read()} reads data from a named array in a multidimensional raster
dataset, with optional CF convention decoding (nodata masking,
scale/offset). The returned vector has a \code{gis} attribute containing
dimension names, coordinate values, spatial reference, and bounding box.
}
\note{
Dimensions are returned in R column-major order (reversed from GDAL's
C row-major order). The first GDAL dimension (slowest varying) becomes
the last R dimension.
}
\examples{
\dontshow{if (gdal_version_num() >= gdal_compute_version(3, 1, 0) && isTRUE(gdal_formats("netCDF")$multidim_raster)) withAutoprint(\{ # examplesIf}
f <- system.file("extdata/byte.nc", package="gdalraster")

## read the full array
data <- mdim_read(f, "Band1")
str(data)
attr(data, "gis")

## subset read: first 10 rows
sub <- mdim_read(f, "Band1", start = c(0, 0), count = c(10, 20))
length(sub)

## read without CF decoding
raw_data <- mdim_read(f, "Band1", decode = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mdim_info]{mdim_info()}}, \code{\link[=mdim_as_classic]{mdim_as_classic()}}, \code{\link[=mdim_translate]{mdim_translate()}}
}
